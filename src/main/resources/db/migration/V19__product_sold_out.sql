-- ============================================================
--  V19. 상품 수동 품절 컬럼
--
--  관리자가 재고와 무관하게 강제로 품절 처리할 수 있게 한다.
--  실제 품절 판정 = 이 값(sold_out) 이 true 이거나, 재고가 0 이하일 때.
-- ============================================================

ALTER TABLE product
    ADD COLUMN IF NOT EXISTS sold_out boolean NOT NULL DEFAULT false;

-- ============================================================
--  V11. 메인 히어로 사진 설정
--
--  히어로 오른쪽 사진을 관리자 화면에서 바꾸기 위한 설정값이다.
--  스키마 변경은 없고 site_setting 에 행만 추가한다.
--
--  값은 쉼표로 구분한 이미지 주소 목록이다. 순서대로 순환한다.
--    예) /uploads/hero/a.jpg,/uploads/hero/b.jpg
--  비워두면 저장소에 넣어둔 기본 사진을 쓴다.
-- ============================================================

INSERT INTO site_setting (key, value, description) VALUES
    ('main.hero_images', '',
     '메인 상단 사진. 쉼표로 나눠 여러 장을 넣으면 순서대로 바뀝니다. 권장 크기 1600x1200 이상, 세로로 긴 화면에서도 잘리지 않게 가운데에 피사체를 두세요.')
ON CONFLICT (key) DO NOTHING;
